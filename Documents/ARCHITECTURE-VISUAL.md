# Tapin Platform Architecture - Visual Guide

**Date:** November 3, 2025  
**Purpose:** Visual reference for system architecture across all phases

---

## 🎯 Three-Sided Platform Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         TAPIN PLATFORM                           │
│                    (AI-Powered Matching)                         │
└─────────────────────────────────────────────────────────────────┘
                                │
                ┌───────────────┼───────────────┐
                │               │               │
                ▼               ▼               ▼
        ┌───────────┐   ┌───────────┐   ┌───────────┐
        │ 🤝 VOL-   │   │ 🏘️ COMM- │   │ 💼 SERV-  │
        │ UNTEERS   │   │ UNITY     │   │ ICE       │
        │           │   │ NEEDS     │   │ PROVIDERS │
        └───────────┘   └───────────┘   └───────────┘
             │               │               │
             │               │               │
             └───────────────┼───────────────┘
                             │
                    AI MATCHES LOCALS
                      WITH LOCALS
```

**Three User Types:**

- **Volunteers**: Find meaningful opportunities, track hours, get recommendations
- **Community Members**: Post needs, find resources, request services
- **Service Providers**: List services, get urgent alerts, build reputation

---

## 🏗️ Phase 1: Foundation (Current - COMPLETE ✅)

### Simple Two-Tier Architecture

```
┌──────────────────────────────────────────────────────┐
│               USER'S BROWSER                         │
│                                                      │
│  ┌────────────────────────────────────────────┐    │
│  │   React Frontend (Port 5173)               │    │
│  │   - Component-based UI                     │    │
│  │   - Leaflet Map (OpenStreetMap)            │    │
│  │   - Vite hot reload                        │    │
│  └────────────────┬───────────────────────────┘    │
│                   │ HTTP/AJAX                       │
└───────────────────┼─────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────────────────┐
│           BACKEND SERVER                             │
│                                                      │
│  ┌────────────────────────────────────────────┐    │
│  │   Flask API (Port 5000)                    │    │
│  │   - JWT Authentication                     │    │
│  │   - RESTful endpoints                      │    │
│  │   - SQLAlchemy ORM                         │    │
│  └────────────────┬───────────────────────────┘    │
│                   │                                 │
│                   ▼                                 │
│  ┌────────────────────────────────────────────┐    │
│  │   SQLite Database                          │    │
│  │   - users, listings, reviews, ratings      │    │
│  │   - File-based (listings.db)               │    │
│  └────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────┘
```

**Current Features:**

- ✅ User authentication with JWT tokens
- ✅ Create/Read/Update/Delete listings
- ✅ Category filtering (Volunteer, Service, etc.)
- ✅ Interactive map with markers
- ✅ Reviews and ratings (1-5 stars)
- ✅ List/Map toggle view

**Limitations:**

- No AI/smart features
- Simple keyword search only
- Manual data entry required
- Limited scalability (SQLite)

---

## 🤖 Phase 2: AI Infrastructure (Months 1-3)

### Adding Intelligence Layer

```
┌──────────────────────────────────────────────────────────────────┐
│                    USER'S BROWSER                                │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │   React Frontend (same as Phase 1)                     │    │
│  │   NO CHANGES - AI is invisible to users               │    │
│  └────────────────────────┬───────────────────────────────┘    │
│                            │ HTTP/AJAX                          │
└────────────────────────────┼───────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│                   BACKEND API LAYER                              │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │   Flask API (Enhanced)                                 │    │
│  │   - All Phase 1 endpoints                              │    │
│  │   + /api/search?semantic=true  (NEW)                   │    │
│  │   + /api/recommendations/:user_id  (NEW)               │    │
│  │   + /api/urgent-match  (NEW)                           │    │
│  └────────────────┬─────────────────────────┬─────────────┘    │
│                   │                         │                   │
└───────────────────┼─────────────────────────┼───────────────────┘
                    │                         │
        ┌───────────▼────────┐    ┌──────────▼──────────┐
        │  Postgres Database │    │   Redis Message     │
        │  (Migrated from    │    │   Queue             │
        │   SQLite)          │    │                     │
        │  + pgvector ext    │    │  - Task queue       │
        └────────────────────┘    │  - Caching          │
                                  └──────────┬──────────┘
                                             │
                                             ▼
┌──────────────────────────────────────────────────────────────────┐
│                  AI WORKER LAYER (NEW)                           │
│                  (Celery Workers)                                │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Worker 1   │  │   Worker 2   │  │   Worker 3   │          │
│  │              │  │              │  │              │          │
│  │ - Geocoding  │  │ - Embeddings │  │ - Matching   │          │
│  │ - Quality    │  │ - Category   │  │ - Trust      │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                 │                 │                   │
│         └─────────────────┼─────────────────┘                   │
│                           │                                     │
└───────────────────────────┼─────────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────────┐
│                  DATA STORAGE LAYER                              │
│                                                                  │
│  ┌──────────────────┐           ┌──────────────────┐            │
│  │  Pinecone Vector │           │   ML Models      │            │
│  │  Database        │           │   (Local/S3)     │            │
│  │                  │           │                  │            │
│  │  - Embeddings    │           │  - Sentence      │            │
│  │  - Semantic      │           │    Transformers  │            │
│  │    search        │           │  - Classifiers   │            │
│  └──────────────────┘           └──────────────────┘            │
└──────────────────────────────────────────────────────────────────┘
```

**New Capabilities:**

- 🤖 Auto-geocoding addresses
- 🤖 Semantic search (natural language)
- 🤖 Personalized recommendations
- 🤖 Quality scoring
- 🤖 Intelligent categorization

**User Experience:**

- Users see SAME interface
- But get BETTER results
- Faster, smarter matching
- "It just works" magic

---

## 🎯 AI Agent Workflow Example: New Listing

```
1. USER CREATES LISTING
   ├─ POST /api/listings
   ├─ { title, description, address, category... }
   └─ Returns listing_id: 456

2. API QUEUES BACKGROUND TASKS
   ├─ enqueue('enrich_listing', listing_id=456)
   └─ Returns 201 Created immediately (user doesn't wait)

3. AI AGENTS PROCESS (Invisible to User)

   ┌─────────────────────────────────────────────────┐
   │  AGENT 1: Data Enrichment (runs in 30 seconds)  │
   ├─────────────────────────────────────────────────┤
   │  • Geocode address                              │
   │    "123 Main St, Boston" → lat/lng              │
   │  • Extract skills from description              │
   │    "teach coding to kids" → [education, tech]   │
   │  • Generate tags                                │
   │    Auto-tags: youth, STEM, volunteer            │
   │  • Calculate quality score                      │
   │    Completeness: 85/100                         │
   └─────────────────────────────────────────────────┘
                        ↓
   ┌─────────────────────────────────────────────────┐
   │  AGENT 2: Embeddings (runs in 1 minute)         │
   ├─────────────────────────────────────────────────┤
   │  • Generate 384-dim vector from description     │
   │  • Store in Pinecone                            │
   │  • Index for semantic search                    │
   │    Now searchable with:                         │
   │    "help children learn programming"            │
   └─────────────────────────────────────────────────┘
                        ↓
   ┌─────────────────────────────────────────────────┐
   │  AGENT 3: Matching (runs in 2 minutes)          │
   ├─────────────────────────────────────────────────┤
   │  • Find users interested in education           │
   │  • Score match quality (0-100)                  │
   │  • Cache top 20 matches per user                │
   │  • Trigger notifications to top 5               │
   │    "New opportunity matches your interests!"    │
   └─────────────────────────────────────────────────┘

4. LISTING NOW ENHANCED
   ├─ Appears in semantic searches
   ├─ Shows in personalized recommendations
   ├─ Properly geocoded on map
   └─ Auto-categorized correctly
```

**Total Time:** ~3 minutes background processing  
**User Wait Time:** 0 seconds (returns immediately)

---

## ⚡ Urgent Matching Flow (Phase 2)

```
SCENARIO: Venue needs band tonight, current band canceled

1. VENUE POSTS URGENT NEED
   ├─ POST /api/listings
   ├─ { title: "Need rock band TONIGHT 8pm", urgent: true,
   │    needed_by: "2025-11-03T20:00:00Z", budget: "$500" }
   └─ urgency_score: 9/10 (calculated by time until needed)

2. URGENT MATCHING AGENT TRIGGERED (within 30 seconds)

   ┌─────────────────────────────────────────────────────┐
   │  Find Available Providers                            │
   ├─────────────────────────────────────────────────────┤
   │  Query:                                             │
   │  • Category = "band" OR "music"                     │
   │  • Status = "available_now" = TRUE                  │
   │  • Within 30 miles of venue                         │
   │  • Genre matches "rock"                             │
   │                                                     │
   │  Results: 12 bands found                            │
   └─────────────────────────────────────────────────────┘
                        ↓
   ┌─────────────────────────────────────────────────────┐
   │  Score Matches                                       │
   ├─────────────────────────────────────────────────────┤
   │  Band A: Score 94                                   │
   │  • 8 min drive (proximity: 0.95)                    │
   │  • Rock genre exact match (1.0)                     │
   │  • Available now, online 2 min ago (0.98)           │
   │  • Past reliability: 96% (0.96)                     │
   │  • Response time avg: 3 minutes (0.97)              │
   │                                                     │
   │  Band B: Score 87 (similar breakdown...)            │
   │  Band C: Score 85                                   │
   │  ...                                                │
   │                                                     │
   │  Top 5 selected for alert                           │
   └─────────────────────────────────────────────────────┘
                        ↓
   ┌─────────────────────────────────────────────────────┐
   │  Send Real-Time Alerts                               │
   ├─────────────────────────────────────────────────────┤
   │  Push Notification to 5 bands:                      │
   │                                                     │
   │  🎸 URGENT GIG OPPORTUNITY                          │
   │  • Need rock band TONIGHT 8pm                       │
   │  • $500 - 8 minutes away                            │
   │  • Tap to accept or decline                         │
   │                                                     │
   │  First to respond gets the gig!                     │
   └─────────────────────────────────────────────────────┘

3. BAND A RESPONDS IN 4 MINUTES
   ├─ Tap "I'm Available" button
   ├─ Venue gets instant notification
   ├─ Contact info exchanged
   └─ Platform logs: response_time=4min, outcome=booked

4. OTHER BANDS NOTIFIED
   ├─ "This opportunity has been filled"
   ├─ AI learns: Band A is reliable, fast responder
   └─ Future priority boost for Band A
```

**Time to Match:** 4 minutes from post to booking  
**Traditional Method:** Hours of phone calls, maybe never filled

---

## 🚀 Phase 3-5: Full Production Scale

### Distributed Architecture (Months 6-12)

```
┌──────────────────────────────────────────────────────────────────────┐
│                         USERS (10,000+)                              │
└────────────────────────────────┬─────────────────────────────────────┘
                                 │
                                 ▼
┌──────────────────────────────────────────────────────────────────────┐
│                     CLOUDFLARE CDN                                   │
│                  (Static Assets + DDoS Protection)                   │
└────────────────────────────────┬─────────────────────────────────────┘
                                 │
                                 ▼
┌──────────────────────────────────────────────────────────────────────┐
│                   LOAD BALANCER (nginx)                              │
│              (Round-robin, health checks, SSL termination)           │
└────────────────────────────────┬─────────────────────────────────────┘
                                 │
                ┌────────────────┼────────────────┐
                │                │                │
                ▼                ▼                ▼
        ┌──────────┐     ┌──────────┐     ┌──────────┐
        │ Flask    │     │ Flask    │     │ Flask    │
        │ API      │     │ API      │     │ API      │
        │ Server 1 │     │ Server 2 │     │ Server 3 │
        └────┬─────┘     └────┬─────┘     └────┬─────┘
             │                │                │
             └────────────────┼────────────────┘
                              │
                ┌─────────────┴─────────────┐
                │                           │
                ▼                           ▼
      ┌──────────────────┐        ┌──────────────────┐
      │  Postgres        │        │   Redis          │
      │  Primary         │        │   Cluster        │
      │                  │        │                  │
      │  ├─ Write        │        │  ├─ Sessions     │
      │  └─ Replicas (2) │        │  ├─ Cache        │
      │     (Read-only)  │        │  └─ Task Queue   │
      └──────────────────┘        └────────┬─────────┘
                                           │
                              ┌────────────┴────────────┐
                              │                         │
                              ▼                         ▼
                    ┌──────────────────┐    ┌──────────────────┐
                    │   Celery Workers │    │   Scheduler      │
                    │   (Auto-scaling) │    │   (Apache        │
                    │                  │    │    Airflow)      │
                    │  ├─ Worker 1-10  │    │                  │
                    │  └─ 6 AI Agents  │    │  - Daily jobs    │
                    └────────┬─────────┘    │  - Monitoring    │
                             │              └──────────────────┘
                             │
                ┌────────────┴────────────┐
                │                         │
                ▼                         ▼
      ┌──────────────────┐    ┌──────────────────┐
      │  Pinecone        │    │  ML Model        │
      │  Vector DB       │    │  Storage         │
      │  (Managed SaaS)  │    │  (S3 + Local)    │
      └──────────────────┘    └──────────────────┘
```

**New Components:**

- **Load Balancer**: Distributes traffic across multiple API servers
- **Postgres Replicas**: Read-heavy queries go to replicas
- **Redis Cluster**: High-availability caching and queuing
- **Auto-scaling Workers**: Spin up more workers during peak load
- **Apache Airflow**: Orchestrates complex AI workflows
- **Monitoring**: Prometheus + Grafana + Sentry

**Capabilities at Scale:**

- 10,000+ concurrent users
- 99.9% uptime
- <100ms API response time
- Real-time notifications to thousands
- Multi-city deployment

---

## 📊 Data Flow Diagram

### Semantic Search Example

```
USER TYPES: "help homeless people"
        ↓
┌─────────────────────────────────────────┐
│  Frontend                               │
│  • Capture input                        │
│  • Show loading spinner                 │
└────────────────┬────────────────────────┘
                 │
                 ▼ GET /api/search?q=help+homeless+people&semantic=true
┌─────────────────────────────────────────┐
│  Flask API                              │
│  • Parse query params                   │
│  • Trigger semantic search              │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  NLP Agent                              │
│  • Tokenize: ["help", "homeless", ...] │
│  • Identify intent: volunteer           │
│  • Extract entities: beneficiary=homeless│
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  Embeddings Model                       │
│  • Convert text → 384-dim vector        │
│  • [0.23, -0.45, 0.89, ...]             │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  Pinecone Vector Database               │
│  • Cosine similarity search             │
│  • Find closest 100 listings            │
│  • Return with similarity scores        │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  Re-ranking Algorithm                   │
│  • Apply filters (location, date)       │
│  • Boost recent listings                │
│  • Factor in quality score              │
│  • Return top 20                        │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  Flask API Response                     │
│  • Format results JSON                  │
│  • Include match reasoning              │
│  • Return in <500ms                     │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  Frontend Display                       │
│  • Render listing cards                 │
│  • Show "Because you searched for..."   │
│  • Highlight matching keywords          │
└─────────────────────────────────────────┘

RESULT: User sees listings about:
• "Serve meals at homeless shelter"
• "Volunteer at housing assistance org"
• "Donate supplies to transitional housing"

Even though descriptions don't contain exact phrase "help homeless people"!
```

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        SECURITY LAYERS                       │
└─────────────────────────────────────────────────────────────┘

LAYER 1: Network Security
├─ Cloudflare DDoS protection
├─ SSL/TLS encryption (HTTPS only)
├─ Rate limiting (100 req/min per IP)
└─ Firewall rules (allow only 443, 80)

LAYER 2: Application Security
├─ JWT token authentication (15-min expiry)
├─ Bcrypt password hashing (12 rounds)
├─ SQL injection prevention (SQLAlchemy ORM)
├─ XSS protection (React escaping + CSP headers)
└─ CSRF tokens on state-changing requests

LAYER 3: Data Security
├─ Postgres row-level security (RLS)
├─ Encrypted at rest (AES-256)
├─ Encrypted in transit (TLS 1.3)
├─ PII data minimization
└─ GDPR-compliant data retention

LAYER 4: AI Security
├─ Input sanitization (no code injection)
├─ Output validation (no PII leakage)
├─ Model access controls (API keys)
├─ Audit logs for all AI decisions
└─ Bias detection and mitigation

LAYER 5: Operational Security
├─ Regular security audits
├─ Dependency vulnerability scanning
├─ Secrets in environment variables (never committed)
├─ 2FA for admin accounts
└─ Incident response plan
```

---

## 📱 Mobile App Architecture (Future - Phase 6)

```
┌─────────────────────────────────────────┐
│       React Native Mobile App           │
│       (iOS + Android)                   │
│                                         │
│  ┌───────────────────────────────┐     │
│  │  - Push notifications         │     │
│  │  - GPS tracking               │     │
│  │  - Camera (photos)            │     │
│  │  - Calendar integration       │     │
│  │  - Offline mode (SQLite)      │     │
│  └───────────────┬───────────────┘     │
└──────────────────┼─────────────────────┘
                   │
                   ▼ REST API + WebSocket
┌──────────────────────────────────────────┐
│       Same Backend as Web                │
│       (No changes needed!)               │
└──────────────────────────────────────────┘

Additional Features:
• Push notifications via Firebase Cloud Messaging
• Background location tracking for ETA
• Offline-first architecture (sync when online)
• App store deployment (TestFlight → Production)
```

---

## 🎓 For Developers: Quick Reference

### Environment Setup Commands

```bash
# Backend setup
cd backend
python3 -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate
pip install -r requirements.txt
python app.py

# Frontend setup
cd frontend
npm install
npm run dev

# Phase 2: Add Redis + Celery
brew install redis  # or apt-get install redis-server
redis-server
celery -A tasks worker --loglevel=info

# Phase 2: Add Postgres
brew install postgresql  # or apt-get install postgresql
createdb tapin_db
python manage.py db upgrade
```

### Key Files to Know

```
backend/
├── app.py                    # Main Flask app
├── models.py                 # Database models
├── auth.py                   # JWT authentication
├── agents/                   # NEW: AI agents
│   ├── data_enrichment.py
│   ├── matching.py
│   └── urgent.py
└── tasks.py                  # NEW: Celery tasks

frontend/
├── src/
│   ├── components/           # React components
│   ├── pages/                # Page views
│   └── services/             # API calls
```

---

## 📞 Need Help?

- **Architecture Questions:** See [AI-PRODUCT-ROADMAP.md](AI-PRODUCT-ROADMAP.md)
- **Implementation Details:** Check code comments in `backend/agents/`
- **User Stories:** Reference [AI-PRODUCT-ROADMAP.md](AI-PRODUCT-ROADMAP.md) user story IDs
- **Quick Overview:** Read [EXEC-SUMMARY.md](EXEC-SUMMARY.md)

---

**Last Updated:** November 3, 2025  
**Next Update:** After Phase 2 completion
