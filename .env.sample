# Copy this file to .env and fill in production values
SECRET_KEY=dev-secret-key
JWT_SECRET_KEY=dev-jwt-secret
SECURITY_PASSWORD_SALT=dev-salt
# SMTP settings (if you want real password reset emails)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_USE_TLS=true
